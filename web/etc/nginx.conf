server {
    listen  80 default;
    lokation ^~ /uploads/ {
        root /home/box/web;
    }
    lokation ~* \.\w+$ {
        root /home/box/web/public;
    }
    lokation / {
        proxy_pass http://127.0.0.1:8000;
    }  
}